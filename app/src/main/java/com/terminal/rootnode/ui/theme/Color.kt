package com.terminal.rootnode.ui.theme

import androidx.compose.ui.graphics.Color

// ── Nord Polar Night (backgrounds) ─────────────────────────────────────────
val NordNight0   = Color(0xFF1A1B26) // Deepest background (Tokyo-night tint)
val NordNight1   = Color(0xFF1E2030) // Slightly lighter panel background
val NordNight2   = Color(0xFF2E3440) // Card / input background
val NordNight3   = Color(0xFF3B4252) // Border / divider

// ── Nord Snow Storm (foreground text) ──────────────────────────────────────
val NordSnow0    = Color(0xFFD8DEE9) // Primary text — crisp frosted white
val NordSnow1    = Color(0xFFE5E9F0) // Bright white (banner titles)
val NordSnow2    = Color(0xFFECEFF4) // Highlight white

// ── Nord Frost (accents) ───────────────────────────────────────────────────
val NordFrost0   = Color(0xFF8FBCBB) // Soft teal — wolf art
val NordFrost1   = Color(0xFF88C0D0) // Icy cyan — system info / sysinfo boxes
val NordFrost2   = Color(0xFF81A1C1) // Steel blue — secondary accents
val NordFrost3   = Color(0xFF5E81AC) // Deep blue — dividers, prompt prefix

// ── Nord Aurora (semantic colors) ─────────────────────────────────────────
val NordGreen    = Color(0xFFA3BE8C) // Muted emerald — primary prompt / commands
val NordYellow   = Color(0xFFEBCB8B) // Warm amber — battery, disk, warnings
val NordOrange   = Color(0xFFD08770) // Muted orange — errors (softer than red)
val NordRed      = Color(0xFFBF616A) // Desaturated red — critical errors
val NordPurple   = Color(0xFFB48EAD) // Soft lavender — tips, launching, aliases

// ── Input Bar ──────────────────────────────────────────────────────────────
val InputBarDark = Color(0xFF1E2030) // Input pill background

// ── Legacy Material aliases (kept for Theme.kt) ───────────────────────────
val Purple80     = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80       = Color(0xFFEFB8C8)
val Purple40     = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40       = Color(0xFF7D5260)